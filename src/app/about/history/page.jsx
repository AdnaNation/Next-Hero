import React from "react";
export const metadata = {
  title: {
    absolute: "History",
  },
  description: "This is history page",
  keywords: ["history", "hero history"],
};

const page = () => {
  return <div>this is history</div>;
};

export default page;
